// Arrow function to calculate the square of a number
const square = (number) => {
    return number * number;
  };
    let inputNumber = 5;
  // Calculating square using the arrow function
  let result = square(inputNumber);
  console.log(`The square of ${inputNumber} is: ${result}`);
  